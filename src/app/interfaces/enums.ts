export enum EditorType {
  monaco = 'monaco',
  custom = 'custom',
  none = 'none'
}
export enum IdeServices {
  theme = 'themeService',
  tree = 'treeService',
  view = 'viewService',
  component = 'componentService',
  editors = 'editorManagerService',
  cache = 'cacheService'
}
